{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "build": "lerna run build",
    "publish": "lerna publish",
    "bootstrap": "lerna bootstrap --hoist",
    "lerna": "lerna"
  }
}
